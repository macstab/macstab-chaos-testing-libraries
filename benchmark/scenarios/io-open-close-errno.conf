# rule scoped to bench's target (/dev/null) so it does not match the
# harness's own open() calls when loading scenarios.
# Probability 1.0 — deterministic ENOENT injection on every call.
/dev/null:open:ENOENT:1.0
