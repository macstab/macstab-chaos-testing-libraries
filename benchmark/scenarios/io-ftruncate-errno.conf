# rule scoped to bench's target (/tmp/chaos-bench-io.*) so it does not match the
# harness's own ftruncate() calls when loading scenarios.
# Probability 1.0 — deterministic EINVAL injection on every call.
/tmp/chaos-bench-io.*:ftruncate:EINVAL:1.0
