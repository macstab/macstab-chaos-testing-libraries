# rule scoped to bench's target (/tmp/chaos-bench-sf-*) so it does not match the
# harness's own sendfile() calls when loading scenarios.
# Probability 0.0 — selector matches but never fires.
/tmp/chaos-bench-sf-*:sendfile:EIO:0.0
