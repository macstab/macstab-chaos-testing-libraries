# rule scoped to bench's target (/tmp/chaos-bench-io.*) so it does not match the
# harness's own fdatasync() calls when loading scenarios.
# Probability 1.0 — deterministic EIO injection on every call.
/tmp/chaos-bench-io.*:fdatasync:EIO:1.0
