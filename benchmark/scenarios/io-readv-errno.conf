# rule scoped to bench's target (/dev/zero) so it does not match the
# harness's own readv() calls when loading scenarios.
# Probability 1.0 — deterministic EIO injection on every call.
/dev/zero:readv:EIO:1.0
