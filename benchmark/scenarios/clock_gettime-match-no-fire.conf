# Stage 1 scenario: a rule that selector-matches but never fires.
# Probability is 0.0 so the dice roll always fails the trigger; the rule
# scan executes (cost we want to measure) but no effect is applied.
clock_gettime:ERRNO:EINVAL,0.0
