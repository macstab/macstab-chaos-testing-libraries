# rule scoped to bench's target (/dev/null) so it does not match the
# harness's own writev() calls when loading scenarios.
# Probability 1.0 — deterministic EIO injection on every call.
/dev/null:writev:EIO:1.0
