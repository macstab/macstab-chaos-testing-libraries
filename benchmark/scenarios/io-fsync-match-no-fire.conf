# rule scoped to bench's target (/tmp/chaos-bench-io.*) so it does not match the
# harness's own fsync() calls when loading scenarios.
# Probability 0.0 — selector matches but never fires.
/tmp/chaos-bench-io.*:fsync:EIO:0.0
