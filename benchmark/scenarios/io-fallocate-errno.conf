# rule scoped to bench's target (/tmp/chaos-bench-io.*) so it does not match the
# harness's own fallocate() calls when loading scenarios.
# Probability 1.0 — deterministic ENOSPC injection on every call.
/tmp/chaos-bench-io.*:fallocate:ENOSPC:1.0
