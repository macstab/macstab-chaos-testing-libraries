# Stage 1 scenario: deterministic ERRNO injection on every call.
# Probability is 1.0 so the effect dispatch path is exercised every
# iteration (synthetic EINVAL return, errno set).
clock_gettime:ERRNO:EINVAL,1.0
