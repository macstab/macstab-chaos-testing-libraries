# Stage 1 scenario: no chaos rules.
# Used by clock_gettime_baseline when run with LD_PRELOAD set, to measure
# pure passthrough overhead (TLS guard + snapshot pointer load + early
# return).  When run without LD_PRELOAD this file is unused.
